pipeline {
    agent { label 'java' }  // This means the pipeline will run on a node labeled 'java'
     stages {
        stage('Clone Repository') {
            steps {
                // Use the Git command to clone the repository
                git 'https://github.com/dev-sandeep-git/python.git'
            }
        }
        }
        }
